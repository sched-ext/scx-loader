[workspace]
members = ["xtask/", "crates/*"]
resolver = "2"

[profile.release]
lto = "thin"

[profile.release-tiny]
inherits = "release"
lto = "thin"
strip = true
incremental = true
codegen-units = 1

[profile.release-fast]
inherits = "release"
lto = false
incremental = true
